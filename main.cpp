#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>
#include <omp.h>
#include <locale.h>
#include <iostream>
#include <windows.h>


using std::cout;

float solution(float A, float den, int N) {
	
	for (int i = 0; i < N; i++) {
		A *= 2 / sqrt(2)
	}
}

int main()
{
	int N, rank, k, i;
	

	scanf("%d", &N);
	/*if (N != 1) {
		den = sqrt(2);
		for (int i = 0; i < N; i++) {
			A *= 2 / 
		}
	}*/
	
}